创建方法 类似于字典 
1. 使用花括号{}

num2 ={1,2,3,4,24,,526,2,3} #所有的元素都是唯一的，重复的元素会被抵消
而且不支持索引

2. 使用set()函数创建
set1 = set([1,2,3,4,5,5,5])

-可以把列表的重复的内容单一化

num = [1,2,3,4,5,5]
num = list(set(num)) #得到的set是无序的

可变集合
num2.add(6)
num2.remove(5)
1 in num2 #true, 如果是'1'，就是false

不可变集合frozen set

num3 = frozenset([1,2,3,4,5])
